rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // อนุญาตให้ผู้ใช้ที่ login แล้วอ่าน/เขียนได้ทุก collection
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
